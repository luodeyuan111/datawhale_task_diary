当机器人的视觉感受器（看到）一个物体时，他首先应该将原始图片经过机器视觉或在神经网络处理，得到图片中如深度，特征点以及特征点后的语义，用于与数据集中数据进行匹配，才能知道他看到了什么，在知道对方是什么的情况下，将结合机器人当前任务，如拿起纸盒，或者走出房间，进一步发出指令，驱动硬件工作，达到预设目的。


我心中的具身智能应用场所描述：
在大型智能装备的应用方面，如船舶与飞行器，首先通过内部各个传感器得到原始的信息，再将它们加工，得到如世界模型以及数字孪生的智能内核，用于判断装备自身系统内的健康状况，以及对外的外部条件输入，将这些有效信息再经过一定的判断逻辑，构成该智能体的心智组成，用于判断以及行动，如可以驱动内部的小型设备，在物理不连接的情况下执行由大型装备智能体下达的命令，
成为类似机体的小型格式塔结构，进一步在内部独立完成状态维护以及系统运作，对外也能够基于信息作出判断，驱动动力设备或者机械臂，无人机，无人艇，甚至战斗部进行相应，并且能够进一步将信息共享至其他智能装备，形成大型的蜂群网络，该具身智能集体拥有更强大的心智以及信息感知以及处理能力，并拥有更强大的对外行动能力。
